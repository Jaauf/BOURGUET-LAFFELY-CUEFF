# inventory/hosts.ini
# Pour un déploiement local (WSL / machine courante)
[deploy_target]
localhost ansible_connection=local

# Pour un déploiement distant, remplacer par :
# [deploy_target]
# 192.168.x.x ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

---
# requirements.yml
# Installer toutes les dépendances avec :
#   ansible-galaxy install -r requirements.yml

collections:
  - name: community.docker
    version: ">=3.4.0"

  - name: ansible.posix
    version: ">=1.5.0"
